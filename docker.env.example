# MDTutor Docker Environment
DOMAIN=mdtutor.test

# Public Ports (Default 80/443 for production, or higher for local dev)
TRAEFIK_PORT=13100
TRAEFIK_HTTPS_PORT=13443
TRAEFIK_DASHBOARD_PORT=13180

# HTTPS / SSL Configuration (Let's Encrypt)
# Set HTTPS_ENABLED=true and provide an email to enable auto-SSL
HTTPS_ENABLED=false
ACME_EMAIL=your-email@example.com

# Shared Secret for JWT between SSO and Achievement server
SSO_JWT_SECRET=change_me_to_something_secure

# Rails Master Keys (Required for production mode)
# You can generate these or copy from config/master.key
# SSO_MASTER_KEY=...
# ACHIEVEMENTS_MASTER_KEY=...

# Additional Overlays to clone (ecosystem:layer:git_url)
# ADDITIONAL_OVERLAYS=tag:makerspace:https://github.com/my-org/makerspace-layer.git

