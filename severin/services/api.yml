name: api
command: ruby -e "require 'socket'; server = TCPServer.new(3101); loop { client = server.accept; client.puts 'HTTP/1.1 200 OK\n\nOK'; client.close }"
port: 3101
description: Mock API Server for Testing
